# Android Login Automation Test (Appium + Python)

This project contains an Android automation testing script built using Appium + Python with the UiAutomator2 automation engine.
The script is used to perform an automated login on an Android application by entering an email, password, and tapping the LOGIN button.

---

## 🛠 Tech Stack
- Python
- Appium
- Selenium WebDriver
- UiAutomator2

---
## 🚀 How to Run

1. Start the Appium Server:
   appium
2. Make sure your Android device or emulator is connected:
   adb devices
3. Verify that the target application is already installed on the device.
4. Install the required Python dependencies:
    pip install Appium-Python-Client selenium
5. Run the automation script:
    python login.py
6. The script will automatically:
    Launch the Android application
    Enter the email and password
    Tap the LOGIN button
---




